# from django.shortcuts import render

# Create your views here.

# sent notification:
# will take request
# user_id = request.user.id
# filter all notification by user id
# return  all filtered notificaiton using a json format [django has json formatter]
